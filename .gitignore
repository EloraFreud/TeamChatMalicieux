# macOS
.DS_Store

# Claude Code — config machine-locale (allowlist de permissions, pas de secret mais local)
.claude/settings.local.json

# Bridge — artefacts runtime locaux
.bridge/last-sync-report.md
/tmp/bridge-*

# Node / track code (bridge-ui/)
node_modules/
bridge-ui/dist/
bridge-ui/storybook-static/
bridge-ui/.storybook-out/

# Vite temp config files
bridge-ui/vite.config.ts.timestamp-*.mjs
*.timestamp-*.mjs
